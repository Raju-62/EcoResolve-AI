# EcoResolve AI Frontend

Static HTML/CSS/JS prototype. Open `index.html` using a local static server (recommended) while the Spring Boot backend runs on port 8080.

Example: `python -m http.server 5500` from this folder, then open http://localhost:5500.
